# Coordinator Heartbeat

- Goal: full self-hosting and compilation to every documented target.
- Phase: bounded internal LLVM gate, native runtime-provider prerequisite, and authenticated packet-binding adapter complete; the first public native-run admission attempt is safely disabled pending a Gravity-authored provider, descriptor-relative execution, and OS process containment.
- Parent implementation baseline: `6410068` (`Authenticate checked-core reference runtime replay`).
- Repository baseline: verified clean integration checkpoint `2dfbc44` (`Merge reviewed development process stack`); the launcher checkpoint is `1d5d8e0`. OpenJDK 26.0.1 and the pinned Apple ARM64 macOS toolchain remain trusted hosts.
- Current full-language audit: 0/240 documents are full-language complete; 7/240 have no executable owner, public `check` accepts 74/197 accepted fixtures, and 664/1,720 rejected fixtures have specific public diagnostics. This internal runtime-provider prerequisite completes no named FL task.
- Current public proof: core app check/run pass for `.gravity` and `.qst` through the bootstrap-only wrapper. Explicit bounded C, Node 20 ES2022 ESM, and Java 21 modular-JAR target paths execute, but remain Clojure-seed-bound and are not full backend, release, or self-hosting claims.
- Completed predecessor: the pinned hosted-JVM reference runtime contract now authenticates allocation/output effects, capabilities, provider/grant decisions, audit/failure behavior, and checked-core authority. The authenticated checked-core boundary covers literal, quote, local, `do`, `if`, `let`, `str`, and `println`, preserves C6-C10 and C2/C3 origin evidence, performs separate verification replay, and fails closed under hostile input. It still records no target-lowering, release, whole-language, or self-hosting credit.
- Completed slice: only the authenticated checked-core artifact reaches a pinned Gravity-authored C11 builder. The bounded result is genuine target-independent MIR with a four-block conditional CFG, SSA definitions/values/uses, terminator uses, ID-indexed C6-C10 fact and proof tables, explicit runtime-check operations/tokens/guard edges, generated source maps, a partial effect-order graph, a content-bound pass execution record, and a noncircular B1 candidate. A separate Clojure stage0 verifier reconstructs every claimed product and fails closed with complete C15 diagnostics. This advances `FL-P06-T03` only as a bounded partial slice; it does not complete C11 or any backend.
- Completed proof: focused C11 `4/385` plus exact-final pure `1/177`; historical reader `30/1760`; hostile sweep `1/1651`; routing `6/177`; C2/C3/P15 compatibility `9/349`; checked-core/runtime predecessors `10/1181`; public/source predecessors `3/141`; all six repository validators; `git diff --check`; and definitive independent adversarial PASS.
- Completed internal gate: verified C11 MIR reaches the pinned Gravity-authored B3 subset, preserves the C13/C14/B1/pass/fact/proof/target/provenance closure, emits deterministic LLVM for `arm64-apple-macosx14.0.0`, and invokes the real Apple Clang/linker to produce and run a Mach-O executable. Standalone `.gravity`/`.qst` accepted and declared-target rejection fixtures preserve cross-root and extension evidence. Unsupported MIR, target leakage, evidence tamper, hostile paths, and failed publication reject before toolchain execution or final publication.
- Seed boundary at this milestone: unchanged on the public path. Clojure still owns compiler orchestration, evaluator/runtime, verification, artifact construction, process/file I/O, and the packaged CLI entry point. The internal B3 artifact remains `:clojure-seed-boundary? true`, `:public-target? false`, `:self-hosted? false`, and non-release. Formal-language completion remains 0/240.
- Current fail-closed slice: explicit targeted `run` requests bypass a stale packaged JAR and reach the current source boundary, but `gravity run <source> --target c --lowering runtime-derived` rejects before source/staging I/O with `P18T04002` and `:missing-fact :contained-public-native-run`. Focused evidence is 19 tests/147 assertions with no failures; the public command exits 1 and retains `:clojure-seed-boundary? true`. Captured-process cleanup is live-output-bounded, interrupt-preserving, and does not overclaim whole-process-tree proof.
- Completed launcher prerequisite: `bootstrap/native/p15_public_native_launcher.c` is a host-authored Darwin primitive for one identity-bound Mach-O launch. It verifies the suspended child's mapped vnode before release and removes live same-process-group members on timeout, leader exit, or launcher interruption. Deterministic replacement rejects with `P15NL009` before child code; the supervised focused run passed 8 tests/60 assertions and independent review approved the partial contract. The proof explicitly keeps descriptor-relative execution, same-euid mutation resistance, external-`SIGCONT` resistance, OS/full-tree containment, code signing, public routing, self-hosting, and release credit false.
- Completed internal runtime-provider prerequisite: the Gravity-authored contract `bootstrap/gravity/p15_s23/native_runtime_driver.gravity` is implemented by an independently reviewable host-C ARM64 provider. The selected child runs with `PATH=/nonexistent`, imports no process/filesystem/network/dynamic-loader functions, executes accepted content-bound packets, buffers stdout until a valid final halt, and rejects CLI misuse, embedded NUL, invalid UTF-8, noncanonical values, tamper, unsupported operations, value/output overflow, stack faults, halt faults, and bounds with stable `P15NR*` diagnostics. Supervised evidence passed 9 tests/234 assertions and independent review passed. The provider preserves declared `.gravity`/`.qst` paths but cannot independently verify the declared source SHA.
- Completed bounded internal packet-binding slice: the Clojure-hosted adapter authenticates the existing target-neutral stage2 compiler packet against strictly decoded source text, its re-encoded UTF-8 hash, and pinned Gravity rule identities, lowers only supported scalar `str`/`println` operations into the real native-provider wire, executes accepted `.gravity` and `.qst` inputs, and rejects coherent source/path context mismatch, packet/rule/plan tamper, unsupported `if`/`let`, and an authenticated wire-path bound before the child-launch step. The supervised focused run passed 13 tests and 303 assertions after one recorded 900-second timeout and a successful 1,800-second retry. This removes only hand-authored-payload evidence and establishes actual packet consumption; it does not reduce the public or global Clojure boundary or advance backend-boundary or backend-conformance claims. The host-C provider, compiler, verifier, adapter, artifact construction, process/file I/O, and wrapper remain trusted; the only selected-child reduction remains `:selected-runtime-clojure-seed-boundary? false`.
- Public seed boundary at the launcher milestone: unchanged. The launcher is not Gravity-authored and is not invoked by `bin/gravity`; compiler, evaluator/runtime, verifier, artifact construction, process/file I/O, and release wrapper remain Clojure/JVM or host-trusted. Formal-language completion remains 0/240.
- Seed boundary detail: the selected internal native runtime process now records `:selected-runtime-clojure-seed-boundary? false`, but that is not a public reduction. Its provider is host-authored C, while Clojure still constructs/verifies packets and owns compiler, artifact, process/file-I/O, and public-wrapper boundaries. Top-level and public `:clojure-seed-boundary?` remain true; self-hosting/release remain false; formal-language completion remains 0/240.
- Active frontier: replace the host-C provider with a Gravity-authored executable implementation and add OS-contained descriptor-relative execution that tracked `bin/gravity` actually invokes. The authenticated adapter, mapped-vnode/same-process-group launcher, and bounded native provider close separate internal prerequisite subproblems but do not satisfy `:contained-public-native-run`. Source-model, replay, manifest, comparison, generated release-wrapper evidence, and a disabled admission route do not reduce the public boundary.
- Known blockers after this slice: Java `ProcessBuilder` cannot select the executable descriptor-relative to the held secure workspace and `ProcessHandle.descendants` cannot prove whole-tree containment; no Gravity-authored executable can yet consume the current stage2 plan or source artifact on the public path; full reader and language semantics remain partial; most operation and control-flow families are not authenticated checked core or MIR; effectful checked-core authority remains canonical JVM-only; Clojure still seed-compiles and verifies Gravity modules; final P15 seed retirement and P18 release proofs remain false.
- Public C, Node ESM, and opt-in runtime-derived JVM routes are completed and reviewed for the bounded stage2 subset. They share an authenticated target-neutral packet and prove byte-exact differential execution, deterministic/path-neutral identities, transactional target artifacts, and fail-closed diagnostics. Production expression lowering, plan assembly, and the bounded closed-plan interpreter are Gravity-authored; trusted consumers independently replay source/rules and record verification separately from authoritative execution. The target slices are not full B2/B5/B6 conformance or self-hosting evidence and do not change the active public executable seed-boundary frontier.
- Rule: keep each implementation slice narrow, independently reviewed, and honestly scoped.
