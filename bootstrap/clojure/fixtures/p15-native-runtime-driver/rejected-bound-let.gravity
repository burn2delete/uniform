(ns gravity.p15-s23.native-runtime-driver.rejected-bound-let
  (:profile :hosted)
  (:target :c)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe))

(defn main [] (let [message "bad"] (println message)))
