(ns gravity.p15-s23.native-runtime-driver.accepted-branch
  (:profile :native)
  (:target :arm64-macos)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe))

(defn main [] (if false (println "bad") (println "ok")))
