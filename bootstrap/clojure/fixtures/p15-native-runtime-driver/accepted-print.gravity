(ns gravity.p15-s23.native-runtime-driver.accepted-print
  (:profile :native)
  (:target :arm64-macos)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe))

(defn main [] (println "Hello Gravity"))
