(ns gravity.p15-s23.native-runtime-driver.rejected-bound-if
  (:profile :hosted)
  (:target :c)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe))

(defn main [] (println (if true "bad" "worse")))
