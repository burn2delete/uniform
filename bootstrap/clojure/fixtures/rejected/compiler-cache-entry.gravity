(ns compiler.cache-entry
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:cache-entry-overrides
                                  [{:stage :type-check
                                    :diagnostics nil}]}}}))
