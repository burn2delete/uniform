(ns math.opt-simd
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:candidate-overrides [{:simd? true
                                   :lane-certificate nil}]}}}))
