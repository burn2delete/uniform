(ns compiler.checked-core.capability-proof
  (:profile :hosted)
  (:target :jvm)
  (:exports [ready])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:checked-core {:fail :capability-proof}}}))

(defn ready []
  true)
