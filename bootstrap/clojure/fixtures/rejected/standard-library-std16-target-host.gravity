; Rejected P16 STD16 fixture.
(ns standard-library.std16-target-host
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:standard-library {:phase16 {:fail :std16002}}}))

(defn main [] :- I64 1)
