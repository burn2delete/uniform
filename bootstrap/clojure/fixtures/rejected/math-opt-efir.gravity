(ns math.opt-efir
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:subgraph-overrides [{:verified-efir? false}]}}}))
