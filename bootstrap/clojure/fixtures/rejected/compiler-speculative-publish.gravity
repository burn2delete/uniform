(ns compiler.speculative-publish
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:speculative-reuse-overrides
                                  [{:artifact-id
                                    "sha256:stage0-speculative-expansion"
                                    :publishable? true}]}}}))
