(ns compiler.release-gate
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:release-gate-report-overrides
                                  {:evidence-gaps [:build-mir]}}}}))
