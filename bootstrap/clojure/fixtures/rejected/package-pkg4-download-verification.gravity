; Rejected P12 PKG4 fixture.
(ns package.pkg4-download-verification
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/network :filesystem/write})
  (:capabilities #{:package/fetch})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg4001}}}))

(defn main [] :- I64 1)
