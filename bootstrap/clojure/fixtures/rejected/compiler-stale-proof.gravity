(ns compiler.stale-proof
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:proof-reuse-overrides
                                  [{:proof-id :proof/stage0-bounds
                                    :status :stale
                                    :reuse :accepted}]}}}))
