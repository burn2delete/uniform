; Rejected P12 PKG7 fixture.
(ns package.pkg7-uncontrolled-network
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/network})
  (:capabilities #{:package/fetch})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg7003}}}))

(defn main [] :- I64 1)
