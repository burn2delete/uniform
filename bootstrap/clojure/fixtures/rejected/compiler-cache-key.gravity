(ns compiler.cache-key
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:cache-key-overrides
                                  [{:stage :type-check
                                    :source nil}]}}}))
