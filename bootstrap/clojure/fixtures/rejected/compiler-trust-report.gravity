(ns compiler.trust-report
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:compiler-trust-report
                                  {:artifact :gravity/compiler-trust-report
                                   :compiler
                                   :gravity-stage0-clojure-bootstrap
                                   :passes []
                                   :profiles {}
                                   :known-gaps []
                                   :covered-passes []}}}}))
