; Rejected P12 PKG11 fixture.
(ns package.pkg11-implicit-host-target
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/package-index})
  (:capabilities #{:package/resolve})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg11002}}}))

(defn main [] :- I64 1)
