(ns compiler.checked-core.type-verify
  (:profile :hosted)
  (:target :jvm)
  (:exports [ready])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:checked-core {:fail :type-verify}}}))

(defn ready []
  true)
