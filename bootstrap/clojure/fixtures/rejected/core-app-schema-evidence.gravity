(ns core.app.schema-evidence
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:schema {:compiled-gate
                       {:artifact-schemas
                        [{:artifact :gravity/artifact-schema-registry
                          :evidence-schema [:types :effects]}]}}}))

(defn main []
  (println "bad schema evidence"))
