; Rejected P10 S8 fixture.
(ns schema.s8-secret
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:schema/validate :build/env :secrets/read})
  (:capabilities #{:schema/validate :env/read :secret/read})
  (:safety :safe)
  (:metadata {:package gravity/schema-stage0
              :schema {:interop {:fail :s8-secret}}}))

(defn main [] :- I64 8)
