(ns stage1.rejected.ambient-authority
  (:profile :meta)
  (:target :jvm)
  (:exports [main])
  (:effects #{:filesystem/read})
  (:capabilities #{:fs/read})
  (:safety :safe)
  (:metadata {:bootstrap
              {:stage :stage1
               :component :reader
               :owner :gravity-source
               :source-language :gravity
               :seed :clojure-stage0
               :retirement-objective :replace-clojure-seed
               :ambient-authority-denied false
               :documents ["BOOT1" "C2"]
               :implements [:reader]
               :preserves #{:source-spans :syntax-identity
                            :diagnostic-codes :artifact-provenance}
               :lineage {:verified-by :clojure-stage0
                         :next-stage :stage1}}}))

(defn main [] 0)
