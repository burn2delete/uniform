(ns core.app.package-target
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:package {:compiled-gate
                        {:target-matrices
                         [{:artifact-id "target-matrix:bad"
                           :implicit-host-target? true}]}}}))

(defn main []
  (println "bad package target"))
