; Rejected P14 TEST13 fixture.
(ns conformance.test13-provenance
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:conformance {:system {:fail :test13002}}}))

(defn main [] :- I64 1)
