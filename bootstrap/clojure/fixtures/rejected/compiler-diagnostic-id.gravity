(ns compiler.diagnostic-id
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:diagnostic-catalog
                                  [{:rule "C1-PIPELINE"}
                                   {:rule "C1-PIPELINE"}]}}}))
