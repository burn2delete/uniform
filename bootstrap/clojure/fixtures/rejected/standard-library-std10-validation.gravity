; Rejected P16 STD10 fixture.
(ns standard-library.std10-validation
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:standard-library {:phase16 {:fail :std10001}}}))

(defn main [] :- I64 1)
