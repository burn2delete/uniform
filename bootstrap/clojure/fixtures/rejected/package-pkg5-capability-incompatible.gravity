; Rejected P12 PKG5 fixture.
(ns package.pkg5-capability-incompatible
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/package-index})
  (:capabilities #{:package/resolve})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg5002}}}))

(defn main [] :- I64 1)
