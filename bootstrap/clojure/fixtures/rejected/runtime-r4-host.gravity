; Rejected P08 R4 fixture: undeclared or unsupported host runtime behavior.
(ns runtime.r4-host
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:host/error})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:package gravity/runtime-stage0
              :runtime {:managed {:fail :r4-host}}}))

(defn main [] :- I64
  1)
