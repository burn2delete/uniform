(ns compiler.diagnostic-span
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:diagnostic-fixture-overrides
                                  [{:diagnostic-id
                                    "diag/c1-pass-contract-stage0"
                                    :primary {:artifact
                                              :pass-contract/build-mir}}]}}}))
