; Rejected P08 R6 fixture: orphaned or lifecycle-less task.
(ns runtime.r6-task
  (:profile :distributed)
  (:target :jvm)
  (:exports [main])
  (:effects #{:thread/spawn})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:package gravity/runtime-stage0
              :runtime {:concurrency {:fail :r6-task}}}))

(defn main [] :- I64 1)
