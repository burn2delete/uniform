(ns math.proof-provider
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:proof {:provider-overrides
                   [{:imported-through-safe15? false}]}}}))
