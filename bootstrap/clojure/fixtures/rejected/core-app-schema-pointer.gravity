(ns core.app.schema-pointer
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:schema {:compiled-gate
                       {:binary-abi-schemas
                        [{:artifact :gravity/abi-schema
                          :pointer-policy :raw-pointer-stable-record}]}}}))

(defn main []
  (println "bad schema pointer"))
