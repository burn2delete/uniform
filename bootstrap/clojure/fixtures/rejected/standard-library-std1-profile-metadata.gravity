; Rejected P16 STD1 fixture.
(ns standard-library.std1-profile-metadata
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:standard-library {:phase16 {:fail :std1001}}}))

(defn main [] :- I64 1)
