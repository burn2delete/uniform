(ns compiler.c2-encoding
  (:profile :meta)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:c2-reader {:fail :encoding}}}))

(defn main []
  true)
