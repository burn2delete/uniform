(ns compiler.checked-core.core-verify
  (:profile :hosted)
  (:target :jvm)
  (:exports [ready])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:checked-core {:fail :core-verify}}}))

(defn ready []
  true)
