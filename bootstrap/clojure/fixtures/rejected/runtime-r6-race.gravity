; Rejected P08 R6 fixture: unsynchronized shared mutable state.
(ns runtime.r6-race
  (:profile :distributed)
  (:target :jvm)
  (:exports [main])
  (:effects #{:thread/spawn})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:package gravity/runtime-stage0
              :runtime {:concurrency {:fail :r6-race}}}))

(defn main [] :- I64 1)
