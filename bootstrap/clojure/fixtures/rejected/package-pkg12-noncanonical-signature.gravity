; Rejected P12 PKG12 fixture.
(ns package.pkg12-noncanonical-signature
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/sign})
  (:capabilities #{:artifact/sign})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg12002}}}))

(defn main [] :- I64 1)
