(ns math.numeric-branch
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:math {:numeric
                     {:conversion-rules
                      [{:rule-id :complex-no-branch
                        :profile :native
                        :target :llvm-x86-64-linux
                        :source-family :complex
                        :target-family :float
                        :conversion-mode :proof-backed
                        :rounding :nearest-ties-to-even
                        :proof :proof/imaginary-zero}]}}}))
