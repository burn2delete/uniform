; Rejected P12 PKG8 fixture.
(ns package.pkg8-unsafe-audit
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:ffi/call})
  (:capabilities #{:ffi/c})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg8001}}}))

(defn main [] :- I64 1)
