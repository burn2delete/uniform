(ns core.app.tooling-repl-capability
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:tooling {:compiled-gate
                        {:repl-sessions
                         [{:tool-id :gravity-repl
                           :profile :hosted
                           :target :jvm
                           :capability-grants []}]}}}))

(defn main []
  (println "bad tooling repl capability"))
