(ns performance.bad.realtime-vector-lane
  (:profile :native) (:target :jvm) (:effects #{}) (:capabilities #{})
  (:safety :safe-optimized)
  (:metadata {:performance {:claim {:claim-id :perf/bad-vector-lane :profile :native :target :llvm-x86-64-linux :target-features #{:avx2} :input-shape {:element-type :f32} :layout {:buffer :contiguous} :safety-mode :safe-optimized :semantic-proof #{:types-preserved} :safety-proof #{:bounds-preserved} :benchmark {:harness :bench/realtime :environment-fingerprint {:cpu :x86-64}} :artifacts #{:optimized-mir :proof-index :benchmark-report}}
                           :realtime {:suite-id :bad/vector-lane
                                      :vectorization-records [{:record-id :v :optimization :simd-vectorization :loop :l :operation :op :profile :native :target :llvm-x86-64-linux :requires #{:no-overlap :bounds-safe :aligned-or-safe-unaligned} :vector-width 4 :numeric-mode :strict-f32 :intrinsics #{:i} :proofs #{:no-overlap :bounds-safe :aligned-or-safe-unaligned} :lane-plan {:width 4} :alignment-report {:target-supported? true :aligned? true} :tail-handling :scalar-epilogue :intrinsic-map [] :cache-transformation {:benchmark :b :effect-order-preserved? true :synchronization-preserved? true}}]
                                      :latency-contracts [{}]
                                      :check-elision-records [{}]}}}))
(def bad true)
