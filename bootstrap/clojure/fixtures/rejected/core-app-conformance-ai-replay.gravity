(ns core.app.conformance-ai-replay
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:conformance {:compiled-gate
                            {:ai-workflow-eval-reports
                             [{:artifact-id "ai-workflow-report:bad"
                               :replay-traces []}]}}}))

(defn main []
  (println "bad conformance ai replay"))
