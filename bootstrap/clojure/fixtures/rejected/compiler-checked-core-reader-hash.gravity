(ns compiler.checked-core.reader-hash
  (:profile :hosted)
  (:target :jvm)
  (:exports [ready])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:checked-core {:fail :reader-hash}}}))

(defn ready []
  true)
