(ns core.app.tooling-formatter-roundtrip
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:tooling {:compiled-gate
                        {:formatters
                         [{:tool-id :gravity-fmt
                           :reader-round-trip false}]}}}))

(defn main []
  (println "bad tooling formatter roundtrip"))
