; Rejected P16 STD7 fixture.
(ns standard-library.std7-race
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:standard-library {:phase16 {:fail :std7001}}}))

(defn main [] :- I64 1)
