(ns compiler.plugin-output
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:plugin-execution-trace-overrides
                                  [{:pass :plugin/stage0-audit
                                    :verifier-result :failed}]}}}))
