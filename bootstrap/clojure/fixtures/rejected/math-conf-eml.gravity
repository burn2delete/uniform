(ns math.conf-eml
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:eml-replay-overrides [{:replayed? false}]}}}))
