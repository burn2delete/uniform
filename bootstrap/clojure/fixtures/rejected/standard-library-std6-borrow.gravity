; Rejected P16 STD6 fixture.
(ns standard-library.std6-borrow
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:standard-library {:phase16 {:fail :std6002}}}))

(defn main [] :- I64 1)
