(ns math.opt-proof
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:candidate-overrides [{:proofs []}]}}}))
