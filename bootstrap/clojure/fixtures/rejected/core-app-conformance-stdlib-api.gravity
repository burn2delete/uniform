(ns core.app.conformance-stdlib-api
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:conformance {:compiled-gate
                            {:standard-library-test-reports
                             [{:artifact-id "stdlib-report:bad"
                               :modules []}]}}}))

(defn main []
  (println "bad conformance stdlib api"))
