(ns compiler.plugin-pass-contract
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:plugin-pass-contract-overrides
                                  [{:pass :plugin/stage0-audit
                                    :output nil}]}}}))
