; Rejected P16 STD20 fixture.
(ns standard-library.std20-stability
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:standard-library {:phase16 {:fail :std20001}}}))

(defn main [] :- I64 1)
