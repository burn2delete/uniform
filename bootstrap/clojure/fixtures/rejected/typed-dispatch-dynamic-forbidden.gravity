(ns typed.dispatch.dynamic-forbidden
  (:profile :kernel)
  (:target :jvm)
  (:exports [bad])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe))

(def bad
  (dispatch/reflective :Closeable :FileHandle :close))
