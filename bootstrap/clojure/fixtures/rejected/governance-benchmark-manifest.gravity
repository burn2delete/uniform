(ns performance.bad.governance-benchmark-manifest
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe-optimized)
  (:metadata {:performance {:claim {:claim-id :perf/bad-benchmark-manifest
                                     :profile :native
                                     :target :llvm-x86-64-linux
                                     :target-features #{:avx2}
                                     :input-shape {:element-type :f32}
                                     :layout {:buffer :contiguous}
                                     :safety-mode :safe-optimized
                                     :semantic-proof #{:types-preserved}
                                     :safety-proof #{:bounds-preserved}
                                     :benchmark {:harness :bench/bad
                                                 :environment-fingerprint {:cpu :x86-64}}
                                     :artifacts #{:optimized-mir
                                                  :proof-index
                                                  :benchmark-report}}
                           :governance {:suite-id :bad/benchmark-manifest
                                        :benchmarks [{:benchmark-id :bench/missing-fields
                                                      :profile :native
                                                      :target :llvm-x86-64-linux}]
                                        :pgo-records [{}]
                                        :autotuning-records [{}]}}}))

(def bad true)
