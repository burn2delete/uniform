(ns compiler.checked-core.unresolved-binding
  (:profile :hosted)
  (:target :jvm)
  (:exports [ready])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:checked-core {:fail :unresolved-binding}}}))

(defn ready []
  true)
