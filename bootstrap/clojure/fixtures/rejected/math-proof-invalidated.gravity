(ns math.proof-invalidated
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:proof {:claim-overrides [{:status :invalidated
                                      :invalidated? true}]}}}))
