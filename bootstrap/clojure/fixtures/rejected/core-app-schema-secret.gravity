(ns core.app.schema-secret
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:schema {:compiled-gate
                       {:typed-configs
                        [{:artifact :gravity/config-schema
                          :redaction-report {:database-url :plain-text}}]}}}))

(defn main []
  (println "bad schema secret"))
