(ns core.app.backend-conformance
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:backend {:compiled-gate
                        {:conformance
                         [{:fixture-id :core-app
                           :artifact-id :artifact/core-app
                           :artifact-manifest-validation :invalid}]}}}))

(defn main []
  (println "bad backend conformance"))
