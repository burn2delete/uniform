; Rejected P09 DOM20 fixture.
(ns domain.dom20-taint (:profile :hosted) (:target :jvm) (:exports [main])
  (:effects #{:domain/coverage}) (:capabilities #{:domain/validate}) (:safety :safe)
  (:metadata {:package gravity/domain-stage0
              :domain {:coverage {:fail :dom20-taint}}}))
(defn main [] :- I64 1)
