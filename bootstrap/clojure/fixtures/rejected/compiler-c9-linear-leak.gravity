(ns compiler.c9.linear-leak
  (:profile :hosted)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:c9-ownership-check {:fail :linear-leak}}}))

(def value 1)
