(ns performance.bad.zero-cost-boxing
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe-optimized)
  (:metadata {:performance
              {:claim
               {:claim-id :perf/zero-cost-boxing
                :profile :native
                :target :llvm-x86-64-linux
                :target-features #{:avx2}
                :input-shape {:element-type :f32}
                :layout {:buffer :contiguous}
                :safety-mode :safe-optimized
                :semantic-proof #{:types-preserved}
                :safety-proof #{:bounds-preserved}
                :benchmark {:harness :bench/zero-cost-bad
                            :environment-fingerprint {:cpu :x86-64}}
                :artifacts #{:optimized-mir
                             :proof-index
                             :benchmark-report}}
               :zero-cost
               {:suite-id :perf2/bad-boxing
                :claims
                [{:claim-id :zc/hidden-boxing
                  :optimization :abstraction-erasure
                  :profile :native
                  :target :llvm-x86-64-linux
                  :abstraction :generic-function
                  :equivalent-form :unboxed-call
                  :source-operation :generic/add
                  :expected-erased-costs #{:boxing}
                  :erased-costs #{}
                  :residual-costs #{:boxing}
                  :semantic-proof #{:same-specialization}
                  :safety-proof #{:numeric-mode-preserved}
                  :proof-ids [:safe15/hidden-boxing]
                  :artifacts #{:before-mir
                               :after-mir
                               :dispatch-report
                               :allocation-audit
                               :boxing-audit
                               :runtime-check-report}
                  :before-ir {:artifact-id :mir/before-hidden-boxing}
                  :after-ir {:artifact-id :mir/after-hidden-boxing}
                  :allocation-audit {:hidden-allocations #{}}
                  :boxing-audit {:hidden-boxing #{:boxing}}
                  :dispatch-specialization-report {:residual-dispatch #{}}
                  :runtime-check-report {:unaccounted-checks #{}}}]}}}))

(def bad true)
