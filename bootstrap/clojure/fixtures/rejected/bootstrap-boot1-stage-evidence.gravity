; Rejected P15 BOOT1 fixture.
(ns bootstrap.boot1-stage-evidence
  (:profile :meta)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:bootstrap {:self-hosting {:fail :boot1001}}}))

(defn main [] :- I64 1)
