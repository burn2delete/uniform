; Rejected P13 T11 fixture.
(ns tooling.t11-check-elision
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:tooling {:experience {:fail :t11003}}}))

(defn main [] :- I64 1)
