; Rejected P12 PKG2 fixture.
(ns package.pkg2-undeclared-effect
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/network})
  (:capabilities #{:package/fetch})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg2001}}}))

(defn main [] :- I64 1)
