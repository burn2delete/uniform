(ns typed.bad.linear
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:resource/open})
  (:capabilities #{:resource/file})
  (:safety :safe))

(def bad
  (let [file (resource/open "tmp/data.txt")]
    true))
