(ns compiler.manifest-gap
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:pipeline-manifest-overrides
                                  {:artifact-graph nil}}}}))
