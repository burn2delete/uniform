; Rejected P12 PKG10 fixture.
(ns package.pkg10-provenance
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{:build/sign})
  (:capabilities #{:artifact/sign})
  (:safety :safe)
  (:metadata {:package {:artifacts {:fail :pkg10001}}}))

(defn main [] :- I64 1)
