(ns math.conf-oracle
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:oracle-overrides [{:trusted? false}]}}}))
