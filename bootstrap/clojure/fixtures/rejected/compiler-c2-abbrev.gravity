(ns compiler.c2-abbrev
  (:profile :meta)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:c2-reader {:fail :abbrev}}}))

(defn main []
  true)
