; Rejected P14 TEST5 fixture.
(ns conformance.test5-unsafe-audit
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:conformance {:system {:fail :test5002}}}))

(defn main [] :- I64 1)
