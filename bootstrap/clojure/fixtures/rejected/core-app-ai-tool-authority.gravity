(ns core.app.ai-tool-authority
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:ai {:compiled-gate
                   {:programs
                    [{:agent-id "support-triage"
                      :human-review-policy :missing}]}}}))

(defn main []
  (println "bad ai tool authority"))
