(ns compiler.c9.linear-double
  (:profile :hosted)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:c9-ownership-check {:fail :linear-double}}}))

(def value 1)
