(ns performance.bad.zero-cost-residual
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe-optimized)
  (:metadata {:performance
              {:claim
               {:claim-id :perf/zero-cost-residual
                :profile :native
                :target :llvm-x86-64-linux
                :target-features #{:avx2}
                :input-shape {:element-type :f32}
                :layout {:buffer :contiguous}
                :safety-mode :safe-optimized
                :semantic-proof #{:types-preserved}
                :safety-proof #{:bounds-preserved}
                :benchmark {:harness :bench/zero-cost-bad
                            :environment-fingerprint {:cpu :x86-64}}
                :artifacts #{:optimized-mir
                             :proof-index
                             :benchmark-report}}
               :zero-cost
               {:suite-id :perf2/bad-residual
                :claims
                [{:claim-id :zc/residual-synchronization
                  :optimization :abstraction-erasure
                  :profile :native
                  :target :llvm-x86-64-linux
                  :abstraction :resource-wrapper
                  :equivalent-form :direct-resource-call
                  :source-operation :resource/use
                  :expected-erased-costs #{:synchronization}
                  :erased-costs #{}
                  :residual-costs #{:synchronization}
                  :semantic-proof #{:same-resource}
                  :safety-proof #{:resource-invariant-preserved}
                  :proof-ids [:safe15/residual-synchronization]
                  :artifacts #{:before-mir
                               :after-mir
                               :dispatch-report
                               :allocation-audit
                               :boxing-audit
                               :runtime-check-report}
                  :before-ir {:artifact-id :mir/before-residual-sync}
                  :after-ir {:artifact-id :mir/after-residual-sync}
                  :allocation-audit {:hidden-allocations #{}}
                  :boxing-audit {:hidden-boxing #{}}
                  :dispatch-specialization-report {:residual-dispatch #{}}
                  :runtime-check-report {:unaccounted-checks #{}}}]}}}))

(def bad true)
