(ns alttype.domain-fact
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:language-facets #{:gravity.facet.schema}}))

(def invalid
  (alttype/domain-fact-error :dropped-schema-facts))
