(ns typed.dispatch.missing
  (:profile :hosted)
  (:target :jvm)
  (:exports [bad])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe))

(def bad
  (dispatch/missing :Closeable :FileHandle :close))
