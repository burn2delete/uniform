(ns compiler.pass-contract
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:contract-overrides
                                  [{:pass :build-mir
                                    :output nil}]}}}))
