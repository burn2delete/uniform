(ns compiler.c10.capability
  (:profile :hosted)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:c10-safety-analysis {:fail :capability}}}))

(def value 1)
