(ns compiler.evidence-drop
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:contract-overrides
                                  [{:pass :optimize-mir
                                    :invalidates [:types]
                                    :regenerates []
                                    :replacement-evidence []}]}}}))
