(ns math.conf-rewrite
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:rewrite-replay-overrides [{:replayed? false}]}}}))
