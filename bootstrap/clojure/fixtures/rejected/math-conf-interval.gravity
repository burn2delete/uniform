(ns math.conf-interval
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:conformance
           {:interval-replay-overrides [{:replayed? false}]}}}))
