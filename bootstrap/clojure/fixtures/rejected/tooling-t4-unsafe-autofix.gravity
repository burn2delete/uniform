; Rejected P13 T4 fixture.
(ns tooling.t4-unsafe-autofix
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:tooling {:experience {:fail :t4003}}}))

(defn main [] :- I64 1)
