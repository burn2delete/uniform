; Rejected P14 TEST6 fixture.
(ns conformance.test6-artifact-manifest
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:conformance {:system {:fail :test6004}}}))

(defn main [] :- I64 1)
