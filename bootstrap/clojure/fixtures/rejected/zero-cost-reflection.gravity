(ns performance.bad.zero-cost-reflection
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe-optimized)
  (:metadata {:performance
              {:claim
               {:claim-id :perf/zero-cost-reflection
                :profile :native
                :target :llvm-x86-64-linux
                :target-features #{:avx2}
                :input-shape {:element-type :f32}
                :layout {:buffer :contiguous}
                :safety-mode :safe-optimized
                :semantic-proof #{:types-preserved}
                :safety-proof #{:bounds-preserved}
                :benchmark {:harness :bench/zero-cost-bad
                            :environment-fingerprint {:cpu :x86-64}}
                :artifacts #{:optimized-mir
                             :proof-index
                             :benchmark-report}}
               :zero-cost
               {:suite-id :perf2/bad-reflection
                :claims
                [{:claim-id :zc/hidden-reflection
                  :optimization :abstraction-erasure
                  :profile :native
                  :target :llvm-x86-64-linux
                  :abstraction :interface-call
                  :equivalent-form :direct-call
                  :source-operation :host/interface
                  :expected-erased-costs #{:reflection}
                  :erased-costs #{}
                  :residual-costs #{:reflection}
                  :semantic-proof #{:same-method-target}
                  :safety-proof #{:host-boundary-preserved}
                  :proof-ids [:safe15/hidden-reflection]
                  :artifacts #{:before-mir
                               :after-mir
                               :dispatch-report
                               :allocation-audit
                               :boxing-audit
                               :runtime-check-report}
                  :before-ir {:artifact-id :mir/before-hidden-reflection}
                  :after-ir {:artifact-id :mir/after-hidden-reflection}
                  :allocation-audit {:hidden-allocations #{}}
                  :boxing-audit {:hidden-boxing #{}}
                  :reflection-audit {:hidden-reflection #{:reflection}}
                  :dispatch-specialization-report {:residual-dispatch #{}}
                  :runtime-check-report {:unaccounted-checks #{}}}]}}}))

(def bad true)
