(ns core.app.unsafe-metadata
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :audited-unsafe))

(defn main []
  (unsafe
    {:reason :missing-audit-fields}
    (println "unsafe")))
