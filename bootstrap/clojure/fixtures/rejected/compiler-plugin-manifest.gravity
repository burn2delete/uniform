(ns compiler.plugin-manifest
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:plugin-manifest-overrides
                                  {:api-version nil}}}}))
