(ns core.app.conformance-backend-artifact
  (:profile :hosted)
  (:target :jvm)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe)
  (:metadata {:conformance {:compiled-gate
                            {:backend-conformance-reports
                             [{:artifact-id "backend-report:bad"}]}}}))

(defn main []
  (println "bad conformance backend artifact"))
