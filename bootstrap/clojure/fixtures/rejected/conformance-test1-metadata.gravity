; Rejected P14 TEST1 fixture.
(ns conformance.test1-metadata
  (:profile :hosted)
  (:target :jvm)
  (:exports [main])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:conformance {:system {:fail :test1001}}}))

(defn main [] :- I64 1)
