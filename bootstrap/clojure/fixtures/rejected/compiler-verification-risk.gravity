(ns compiler.verification-risk
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:compiler {:passes {:risk-overrides
                                  [{:pass :build-mir
                                    :risk nil}]}}}))
