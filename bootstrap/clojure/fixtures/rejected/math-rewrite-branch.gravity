(ns math.rewrite-branch
  (:profile :native)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:math {:proof {:rewrite-rule-overrides
                   [{:branch-compatible? false}]}}}))
