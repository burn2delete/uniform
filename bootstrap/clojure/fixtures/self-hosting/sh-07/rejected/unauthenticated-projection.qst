(ns self-hosting.sh07.reject.unauthenticated-projection
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07
              :sh07 {:expected-rule "C6-VERIFY"
                     :expected-stage :core-lowering
                     :expected-severity :error
                     :expected-remediation "Replay the Gravity template and bind every digest ordinal exactly once."}}))

(def verifier-candidate 1)
