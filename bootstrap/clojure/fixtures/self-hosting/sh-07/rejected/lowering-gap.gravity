(ns self-hosting.sh07.reject.lowering-gap
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07
              :sh07 {:expected-rule "C6-LOWERING-GAP"
                     :expected-stage :core-lowering
                     :expected-severity :error
                     :expected-remediation "Use only the declared SH-07-B1 def, fn, quote, if, do, and literal subset."}}))

(def unsupported-local-binding
  (let [value 1] value))
