(ns self-hosting.sh07.reject.missing-origin
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07
              :sh07 {:expected-rule "C6-ORIGIN"
                     :expected-stage :core-lowering
                     :expected-severity :error
                     :expected-remediation "Preserve exact syntax ids, semantic spans, and generated-origin chains."}}))

(defn generated-function [value]
  1)
