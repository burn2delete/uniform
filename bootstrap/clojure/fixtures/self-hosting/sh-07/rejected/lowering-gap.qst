(ns self-hosting.sh07.reject.lowering-gap
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07
              :sh07 {:expected-rule "C6-LOWERING-GAP"
                     :expected-stage :core-lowering
                     :expected-severity :error
                     :expected-remediation "Use only the declared bounded SH-07 core subset; defer unsupported lowering families to their owning slices."}}))

(def unsupported-destructuring
  (let [[value] [1]]
    2))
