(ns self-hosting.sh07.reject.lowering-gap
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07
              :sh07 {:expected-rule "C6-LOWERING-GAP"
                     :expected-stage :core-lowering
                     :expected-severity :error
                     :expected-remediation "Use only the declared SH-07-B3 def, fn, quote, if, do, simple-symbol sequential let, reference, symbol-headed call, and literal subset."}}))

(def unsupported-loop
  (loop [] 1))
