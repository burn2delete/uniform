; SH-07-B4 rejects a non-vector loop binding form.
(ns self-hosting.sh07-b4.loop-binding-vector-required
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B4
    :fixture :loop-binding-vector-required
    :sh07-b4
    {:expected-rule "C6-CORE-SHAPE"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :loop-binding-vector-required
     :expected-remediation
     "Provide a bounded, delimiter-linked SH-06 form graph with exact core-form shape."}}))

(def invalid-loop
  (loop 1 2))
