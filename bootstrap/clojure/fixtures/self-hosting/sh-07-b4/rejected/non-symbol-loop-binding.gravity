; SH-07-B4 rejects a literal loop binding name.
(ns self-hosting.sh07-b4.non-symbol-loop-binding
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B4
    :fixture :non-symbol-loop-binding
    :sh07-b4
    {:expected-rule "C6-CORE-SHAPE"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :loop-binding-symbol-required
     :expected-remediation
     "Provide a bounded, delimiter-linked SH-06 form graph with exact core-form shape."}}))

(def invalid-loop
  (loop [1 2] 3))
