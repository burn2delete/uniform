(ns self-hosting.sh07-b8.reject.protected-sequencing-deferred
  (:profile :meta)
  (:target :jvm)
  (:effects #{:error/throw})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B8
    :sh07-b8
    {:expected-rule "C6-LOWERING-GAP"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :try-protected-sequencing-deferred
     :expected-remediation
     "Wrap protected sequencing in an explicit do form; SH-07-B9 accepts one protected expression."}}))

(def invalid-handler
  (try
    "first"
    (throw "second")
    (catch Exception error
      error)))
