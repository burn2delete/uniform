(ns self-hosting.sh07-b8.reject.finally-remains-deferred
  (:profile :meta)
  (:target :jvm)
  (:effects #{:error/throw})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B8
    :sh07-b8
    {:expected-rule "C6-LOWERING-GAP"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :finally-clause-deferred
     :expected-remediation
     "Omit finally in SH-07-B8; defer cleanup lowering to its owning slice."}}))

(def invalid-handler
  (try
    (throw "cleanup")
    (catch Exception error
      error)
    (finally
      "cleanup")))
