(ns self-hosting.sh07-b8.reject.empty-protected-expression
  (:profile :meta)
  (:target :jvm)
  (:effects #{:error/throw})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B8
    :sh07-b8
    {:expected-rule "C6-CORE-SHAPE"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :try-protected-expression-required
     :expected-remediation
     "Provide exactly one protected expression before the typed catch clause."}}))

(def invalid-handler
  (try
    (catch Exception error
      error)))
