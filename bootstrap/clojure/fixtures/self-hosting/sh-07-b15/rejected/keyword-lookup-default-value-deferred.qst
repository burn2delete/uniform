; SH-07-B15 defers the two-operand keyword default-value form.
(ns self-hosting.sh07-b15.reject.keyword-lookup-default-value-deferred
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B15
    :case :keyword-lookup-default-value-deferred
    :governing-contracts ["L2" "C6"]
    :sh07-b15
    {:expected-rule "C6-CORE-SHAPE"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :keyword-map-lookup-arity
     :expected-actual-operand-count 2
     :expected-required-operand-count 1
     :expected-fail-closed true
     :expected-remediation
     "Provide exactly one map operand to a keyword lookup."}}))

(def contract
  {})

(def required-fields
  (:required-fields contract []))
