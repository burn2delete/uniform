(ns self-hosting.sh07-b9.reject.missing-catch
  (:profile :meta)
  (:target :jvm)
  (:effects #{:error/throw})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B9
    :sh07-b9
    {:expected-rule "C6-CORE-SHAPE"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :try-catch-required
     :expected-remediation
     "Provide exactly one protected expression followed by one or more typed catch clauses."}}))

(def invalid-handler
  (try
    (throw "unhandled")))
