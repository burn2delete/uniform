(ns self-hosting.sh05.reject.capture
  (:profile :meta) (:target :jvm) (:effects #{}) (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-05
              :compiler {:sh05-request
                         {:policy {:capture-policy :implicit
                                   :requested-capabilities [:compiler/authority]
                                   :granted-capabilities [:compiler/authority]}}}
              :sh05 {:expected-rule "C4-CAPTURE" :expected-stage :macro-expansion
                     :expected-severity :error :expected-remediation :remove-or-authorize-capture}}))
(defn candidate [x] x)
