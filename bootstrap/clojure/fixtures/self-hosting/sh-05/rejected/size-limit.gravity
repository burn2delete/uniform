(ns self-hosting.sh05.reject.size
  (:profile :meta) (:target :jvm) (:effects #{}) (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-05
              :compiler {:sh05-request {:policy {:output-node-count 4097}}}
              :sh05 {:expected-rule "C4-SIZE" :expected-stage :macro-expansion
                     :expected-severity :error :expected-remediation :reduce-expansion-size}}))
(defn candidate [x] x)
