(ns self-hosting.sh05.reject.profile
  (:profile :meta) (:target :jvm) (:effects #{}) (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-05
              :compiler {:sh05-request
                         {:context {:profile :kernel}
                          :policy {:allowed-profiles [:hosted]
                                   :generated-profile :hosted}}}
              :sh05 {:expected-rule "C4-PROFILE" :expected-stage :macro-expansion
                     :expected-severity :error :expected-remediation :generate-profile-legal-syntax}}))
(defn candidate [x] x)
