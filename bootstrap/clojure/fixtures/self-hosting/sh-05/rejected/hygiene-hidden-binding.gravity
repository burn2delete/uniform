(ns self-hosting.sh05.reject.hygiene
  (:profile :meta) (:target :jvm) (:effects #{}) (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-05
              :compiler {:sh05-request
                         {:policy {:hidden-captures [generated-binding]}}}
              :sh05 {:expected-rule "C4-HYGIENE" :expected-stage :macro-expansion
                     :expected-severity :error :expected-remediation :introduce-fresh-identifier}}))
(defn candidate [x] x)
