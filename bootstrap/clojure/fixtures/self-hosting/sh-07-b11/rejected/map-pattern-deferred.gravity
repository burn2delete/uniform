; SH-07-B11 does not claim map-pattern lowering.
(ns self-hosting.sh07-b11.reject.map-pattern-deferred
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B11
    :case :map-pattern-outside-fixed-vector-slice
    :governing-contracts ["L7" "C6"]
    :sh07-b11
    {:expected-rule "C6-LOWERING-GAP"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :composite-pattern-deferred
     :expected-remediation
     "Use a fixed-width vector, wildcard, supported scalar literal, or one symbol binding in SH-07-B11; defer other composite patterns to their owning slices."}}))

(def invalid-match
  (match {:kind :value}
    {:kind kind} kind
    _ nil))
