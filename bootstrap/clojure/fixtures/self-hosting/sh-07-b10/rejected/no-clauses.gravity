(ns self-hosting.sh07-b10.reject.no-clauses
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B10
    :sh07-b10
    {:expected-rule "C6-CORE-SHAPE"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :match-clause-required
     :expected-remediation
     "Provide at least one pattern/expression clause pair after the scrutinee."}}))

(def invalid-match
  (match :value))
