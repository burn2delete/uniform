; SH-07-B5 same-namespace var object reference.
(ns self-hosting.sh07-b5.same-namespace-var-object
  (:profile :meta)
  (:target :jvm)
  (:exports [target target-object callable callable-object])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07-B5
              :fixture :same-namespace-var-object}))

(def target 41)

(def target-object
  (var target))

(defn callable [] 7)

(def callable-object
  (var callable))
