(ns self-hosting.sh07-b5.reject.lexical-var-target
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata
   {:slice :SH-07-B5
    :sh07-b5
    {:expected-rule "C6-VERIFY"
     :expected-stage :core-lowering
     :expected-severity :error
     :expected-reason :var-top-level-namespace-binding-required
     :expected-remediation
     "Preserve source spans, syntax identity, generated origins, and authenticated SH-06 binding identity."}}))

(defn invalid-var [value]
  (var value))
