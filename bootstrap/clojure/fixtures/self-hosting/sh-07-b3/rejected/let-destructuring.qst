(ns self-hosting.sh07-b3.reject.let-destructuring
  (:profile :meta)
  (:target :jvm)
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07-B3
              :sh07-b3 {:expected-rule "C6-LOWERING-GAP"
                        :expected-stage :core-lowering
                        :expected-severity :error
                        :expected-reason :let-destructuring-deferred
                        :expected-remediation "Use only the declared SH-07-B4 fixed-arity fn, simple-symbol sequential loop, tail recur, and previously supported core subset."}}))

(def destructured-let
  (let [[first-value] [1]]
    2))
