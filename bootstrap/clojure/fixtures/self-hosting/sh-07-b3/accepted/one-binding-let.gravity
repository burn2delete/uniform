; SH-07-B3 one immutable lexical binding.
(ns self-hosting.sh07-b3.one-binding-let
  (:profile :meta)
  (:target :jvm)
  (:exports [one-binding])
  (:effects #{})
  (:capabilities #{})
  (:safety :safe)
  (:metadata {:slice :SH-07-B3 :fixture :one-binding-let}))

(def one-binding
  (let [value 11]
    value))
