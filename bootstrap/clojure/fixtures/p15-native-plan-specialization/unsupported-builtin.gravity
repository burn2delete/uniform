(ns gravity.p15-s23.native-plan-specialization.unsupported-builtin
  (:profile :hosted)
  (:target :c)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe))

(defn main [] (println (= 1 2)))
