(ns gravity.p15-s23.native-plan-specialization.accepted-bool
  (:profile :hosted)
  (:target :c)
  (:effects #{:io/write})
  (:capabilities #{:io/stdout})
  (:safety :safe))

(defn main [] (println true))
